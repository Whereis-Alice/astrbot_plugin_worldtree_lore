# 更新日志

## v1.7.0

三件事：生效中的条目可以立刻叫停、新增角色卡式的「角色条目」模板、条目可以为自己指定模型或提供商。

**变更**

- 新增 `世界树 终止 [名称或模板]` 命令（别名 `停止`、`熄灭`，限管理员）。此前条目一旦命中就只能等时长和次数耗尽，或者用 `清理` 把整个会话的运行态一起清掉。现在可以不带参数结束当前会话的全部生效条目，也可以只写一个条目名或模板名精确掐掉一类。终止只截断这一次生效、不写入屏蔽表，所以 `re:.*` 这类全量触发的条目下一条消息就会回来——命令回复会检测这种情况并提示改用 `屏蔽`。
- 新增第七种模板「角色条目」（`character`），对应酒馆式的角色卡：预设优先级 5、生效时长 1800 秒、不限次数、关键词留空自己填。它和其它模板共用同一套判定逻辑，区别在注入方式——角色条目单独包一层 persona 说明，和当背景资料用的普通条目分成两块，先写角色再写资料，两块共享同一份字符预算。
- 角色条目默认注入 `system_prompt`（新配置项 `character_injection_target`，也可选 `extra_user_content` 或 `follow` 跟随通用注入位置），因为「机器人是谁」需要比背景资料更高的权重。
- 同一会话默认只保留一个角色条目（新配置项 `single_character_entry`）：新角色条目生效时自动结束上一个，避免两份人格互相打架。普通条目的同优先级共存规则不受影响。
- 条目新增两个可选字段 `model`（模型名）与 `provider`（提供商 ID），只在条目生效期间起作用，条目退场后自动回到 AstrBot 的默认设置；多个条目同时生效时由优先级最靠前的那个决定。动机是有些模型会把角色设定本身当成违规内容拦掉——同一张角色卡在 Gemini 上返回 `400 PROHIBITED_CONTENT`（`prompt_blocked`），在 DeepSeek 上却完全正常，而报错不会指出是哪个词触发的。给这一个条目单独换一个宽松的提供商即可，不必改全局配置，也不用逐字删设定。可用新配置项 `enable_model_switch` 整体关闭。
- 模型名走本轮请求的 `req.model`；提供商必须更早决定，因为 AstrBot 在构建主 agent 之前就选好了 provider。为此新增 `on_waiting_llm_request` 钩子，在那之前写入 `selected_provider`。该钩子返回真值会取消整轮 LLM 请求，所以实现全程 try/except 只记日志，并在写入前用 `get_provider_by_id` 校验：ID 不存在就写一条警告日志并继续用默认提供商，而不是让这一轮请求失败。
- 因此本轮的激活判定可能提前发生在 waiting 钩子里。事件上会打一个标记，`on_llm_request` 看到标记就复用已激活的结果，不会把时长和次数算两次；第三方 agent 路径不触发 waiting 钩子，`on_llm_request` 仍是权威入口。
- `世界树 会话状态` 现在会显示每个生效条目的模板，以及它带的模型 / 提供商覆盖；`世界树 帮助` 补上终止命令。
- 管理台：编辑抽屉新增「模型覆盖（可选）」分区，提供商输入框通过新接口 `GET /providers` 从运行中的 AstrBot 拉取真实 ID 作候选（拉取失败静默降级成纯文本输入）；条目卡片新增模型和提供商徽章；角色条目有独立的紫色模板色（亮 / 暗两套）；按条目类型排序时角色条目排在最前；没填触发词的角色条目会提示它只能靠 `世界树 固定` 激活。
- README 同步补上终止命令、第七种模板、「角色条目与模型覆盖」一节和三个新配置项。

**测试**

- `tests/test_core.py` 新增 `CharacterEntryTests` 六项：model / provider 字段的读写与去空白、超长模型名被拒、`library.update` 能持久化这两个字段、按类型排序时角色条目在最前、同会话角色条目互斥且不影响普通条目、`deactivate` 之后条目仍可重新触发且屏蔽表为空。
- `tests/test_webui_contract.py` 补两项：角色条目能覆盖应答模型、生效中的条目能被随时终止；模板遍历加上 `character`。
- 门禁：38 项单元测试、`node --check pages/worldtree/app.js`、`ruff check .` 全绿。

## v1.6.3

插件标识重做：原来的图标和页头徽章是从背景世界树直接缩下来的，构图元素太多，缩到插件卡片和浏览器标签页的尺寸就糊成一团绿。这版换成为小尺寸重新设计的徽章。

**变更**

- `assets/logo.svg` 重绘为 512×512 的轴对称徽章：双同心环收边，中央五段树干由下往上收窄，五对一级枝再各自二次分叉，枝端共 44 个发光节点，基部有一颗核，环内散着浮尘。轴对称的构图在 64×64 下仍能看出是一棵树，而此前的写实构图在这个尺寸只剩色块。
- 根目录 `logo.png` 按新徽章重新导出为 512×512 透明底圆角图。上一版为了适配四种卡片底色用了满幅深绿底，但圆角透明图在浅色、米黄、深色卡片上都能贴合，不需要自带底色。
- `pages/worldtree/favicon.svg` 改为徽章的精简版，只留 7 个节点，去掉浮尘和二级分叉，在 16px 下可辨。
- 管理台页头的 `.brand-mark` 换成同一套矢量，描边和节点颜色改由 CSS 变量驱动，三个主题各自取对应的林绿色，不再需要为暗色单独写覆盖规则。

**修复**

- 顶部工具带叶冠 `.band-crest` 的上边缘是一条硬边，在浅色主题下像贴了一块色卡。把 `.crest-base` 的不透明度从 0.92 降到 0.64，叶冠整体加 `opacity: .86`，并把遮罩改成上下双向渐隐。
- 删掉 `.brand-mark` 的暗色描边覆盖规则。新徽章没有 `.trunk` / `.branches` / `.roots` / `.ground` 这些子元素，这四条选择器已经匹配不到任何节点。
- 修 `tests/visual_harness.html`：生产页的主题是由 `<head>` 里的内联脚本在首帧前写入的，而这个测试台只把 index.html 的 `<body>` 导进来，`<head>` 脚本被丢掉，于是首帧总是浅色，随后 app.js 切主题会启动上百个颜色过渡。无头浏览器的虚拟时钟下这些过渡停在起始值，截图里就拍到了荧夜主题下呈白底淡字的按钮。现在测试台会镜像同一段主题引导。

**测试**

- 更新 `assets/` 下三张 README 截图：白昼主题管理台、条目编辑抽屉、荧夜主题。
- 新徽章按 16px（标签页）、64px（插件卡片）、128px（README）三档核对过辨识度。

## v1.6.2

README 重写。上一版是 27 个平级章节堆在一起，读者要翻到中段才知道这插件怎么用。

**变更**

- 开头改为一句话定位加一张管理台截图，紧接着「为什么再做一个」，第一屏就能判断要不要装。
- 新增目录，并把章节按读者的任务重排：安装 → 快速上手（五步建出第一条条目）→ 条目怎么生效 → 管理台 → 聊天命令 → 迁移 → 参考手册 → 常见问题。原先模板、关键词、正则、范围、Cron、占位符六个语法章节连排在命令和问答之前，现在统一下沉到参考部分。
- 「主要特点」的 14 条长 bullet 压成四条（找得到 / 看得清 / 改得动 / 搬得走），并补一张表说明条目的四个管理入口，回答「是不是只能在管理台里改」。
- 删掉「为什么条目多了也容易找」「已修复或改进的上游问题」两节自我论证式内容；其中真正影响使用的行为差异保留为迁移一节下的短清单，完整记录指向 CHANGELOG。
- 新增 `assets/` 下三张截图：白昼主题的管理台、条目编辑抽屉、荧夜主题。截图用 `tests/visual_harness.html` 的假数据拍摄，不含真实会话内容。

**修复**

- 文档里写着「导入、导出入口位于筛选区域」，实际入口是顶部工具带的「移植」菜单。
- 补上此前漏记的命令别名：`帮助`/`help`、`查找`/`搜索`、`全局开关`/`开关`。

## v1.6.1

补上插件图标：AstrBot 插件市场和插件管理页此前显示的是默认占位图标。

**变更**

- 新增根目录 `logo.png`（512×512）。AstrBot 只认插件根目录下这个固定文件名，读到之后会填进插件元数据并显示在插件卡片上，`metadata.yaml` 不需要额外字段。
- 图标沿用背景世界树的构图与配色：撑满画面的树冠由六片叶簇叠出轮廓，主干是一根柱状的树体，向下张开成板根并延伸出画外，四周有光晕和飘浮的荧光点。
- 底色做成满幅的深林绿渐变，而不是透明背景。插件卡片有四种底色（浅色 `#ffffff`、深色 `#282833`、市场浅色 `#f8f0dd`、市场深色 `#3a3425`），透明底的图标在米黄和深色卡片上会缺形。
- 矢量源码留档在 `assets/logo.svg`，需要改配色或重新出图时不必从位图反推。
- WebUI 新增 `pages/worldtree/favicon.svg`：同一套造型的简化版，浏览器标签页不再显示空白图标。

**测试**

- 新增契约测试：断言根目录存在 `logo.png`、是合法 PNG、宽高相等且不小于 256，并断言管理台页面引用了 favicon。
- 图标按 64×64（插件卡片实际显示尺寸）分别贴在四种卡片底色上核对过可读性。

## v1.6.0

背景的世界树从「装在画框里」改成占满整个视口：树冠撑开画面上半，树干一路延伸到视口之外，看不到树基。

**变更**

- 背景构图改为覆盖裁切：世界树按视口高度铺满并居中裁切。此前用的是等比缩放，无论窗口多大都会把整棵树完整装进画面，所以树永远显得很小、像一张贴图。现在分叉点落在约 56% 高度处，树干画到画面之外。
- 树干重新按锥度曲线生成：顶端很细、直接藏进枝丛，向下按幂函数张开，到画面底缘约 425px 宽，形成仰视透视。附带竖向树皮纹理与外张的板根，窄屏下整体再拔高一截。
- 树干着色加入空气透视，上端偏浅、下端压到最深的墨绿；枝干与根须加粗并换成更绿的描边色，树冠叶层的不透明度一并提高，避免整棵树在浅色背景上发灰。
- 雾层与地面层压低并简化，不再暗示地平线，视线可以顺着树干直接出画。
- 新增飘落的叶片点缀：七片叶子以不同速度下落、左右微摆并旋转，首尾淡出。荧夜主题下改为青绿色并带一点微光。开启系统的「减少动态效果」时不播放。
- 荧夜主题下树干原本几乎被背景吞掉，改为深青底色配发光的竖向树脉。
- 树干再加宽一档，画面底缘处约 520px（1440 宽下）。之前的锥度到了视口底还是偏细，仰视的压迫感不够。
- 左右留白里新增垂下的气根与叶簇。内容列是居中定宽的，居中的树干几乎整根被卡片挡住，能读到尺度的其实只有两侧留白；三对粗细不同的气根从画面上方垂到视口之外，配合渐隐遮罩淡入，不会出现悬空的线头。

**测试**

- 补测了 390 宽的窄屏布局。此前看起来像是内容被裁切，实际是 Windows 上 Edge 的窗口有约 500px 的最小宽度，直接用 `--window-size=390` 截图会先按 496px 排版再裁成 390，属于截图工具的假象；用真实 390 视口复测，顶部工具带、状态标签与分页控件都正常换行。

## v1.5.0

管理台重做了布局与材质：左侧栏合并进顶部的树冠工具带，条目列表回到画面中央，所有面板改成半透明的叶片玻璃。

**变更**

- 移除左侧栏，把树冠概览、搜索、筛选、排序与视图开关合并为顶部一条「树冠工具带」：带子上缘长出叶簇，下缘由一根主干延伸到条目列表。原本被侧栏占掉的横向空间还给了条目本身，宽屏下正文区域明显变宽。
- 条目列表改为居中定宽，不再随窗口无限拉长；1440 / 1660 / 1920 宽度下都按同一套断点排版。
- 面板改用「叶片玻璃」：半透明底色加毛玻璃模糊、斜向叶脉纹理，以及一角削平的叶形轮廓，背后的世界树可以透出来。白昼、暗林、荧夜三套配色各有一组独立的玻璃参数，正文对比度仍满足 WCAG AA。
- 更细的筛选条件（状态、模板、文件夹、标签）默认收起，按钮上显示当前生效的条件数量，避免顶部工具带过高。
- 背景世界树补齐根须并降低雾层高度，树冠年轮的透明度从 0.7 降到 0.42。

**修复**

- 宽屏下的布局塌陷：样式表此前只有一个 900px 断点，标题行的两端对齐会在宽屏上把元素推成「两座孤岛」，分组标题的展开箭头甚至会被推到 1500px 之外。
- 背景上三处模糊的绿色斑点（浮尘元素）在浅色主题下看起来像脏污，已改为林间光柱。
- 顶部工具带的枝节线条没有和主干对齐，左端悬空。
- 树冠叶簇原本是一排规则圆点，缺少自然的疏密变化；改为按节律生成的 57 片不规则叶子，并对叶尖做了淡出。

**测试**

- `tests/visual_responsive.html` 重做为六个视口（390 / 834 / 1180 / 1440 / 1660 / 1920）的并排预览，便于一次核对所有断点。

## v1.4.0

从上游导入的条目不再沿用另一套关键词约定，正则写法在整个插件里只有一种。

**变更**

- 从上游世界书导入的条目，会把每一行关键词自动改写成 `re:` 前缀形式。上游把关键词整表当正则处理，所以常驻 / 随机 / 群聊限定 / 用户限定条目的默认值是裸 `.*`；改写后写成 `re:.*`，编译出的正则与匹配结果完全一致，只是从此和新建条目共用同一套写法。导入摘要会说明有多少条目被改写。
- 早先版本导入、仍存在数据文件里的兼容模式条目，会在插件启动时做一次性改写（数据版本升到 3），启动日志与管理台的巡检提示会说明改写了多少条。手动选择「旧世界书模式」的条目不会被反复改写。

**新增**

- 编辑器里，仍处于旧世界书模式的条目会给出说明，并提供「改写为 re: 形式」按钮，可单条无损转换。
- 批量操作新增「关键词转为新模式」，一次性处理所选条目；已在新模式下的条目会被跳过。
- 列表卡片上，旧世界书模式的条目带一枚琥珀色徽章，方便找出还没归一的条目。

**修复**

- 旧世界书模式与默认模式并存带来的静默失效：在编辑器里把关键词模式切回默认后，原本作为正则的 `.*` 会变成普通文字，条目从此不再命中，而界面此前没有任何提示。
- 可视化测试台的一条夹具写了无效的关键词模式枚举值（`legacy`），导致旧世界书相关样式无法在测试台里预览。

## v1.3.1

编辑器现在会解释「模板」到底做了什么，并在关键词写法可能出错时提醒。

**新增**

- 编辑条目时，模板下方会说明该模板的预设含义（常规 / 常驻 / 随机 / 日程 / 群聊限定 / 用户限定各有一段说明），触发方式区块也会说明该类条目通常怎么触发。这解释了一个常见疑问：常驻条目为什么也要填触发方式——「常驻」并不是独立的注入通道，它其实是预置了 `re:.*`（匹配任意消息）加上不限时长、不限次数，所以关键词一旦清空，条目就再也不会生效。
- 编辑已有条目时新增「套用该模板默认值」，可主动把当前模板的预设写回触发方式、优先级、生效时长与次数；名称、注入内容、文件夹、标签与适用范围不受影响。切换模板本身仍然只改变分类，不会覆盖已填字段。
- 关键词填成 `.*`、`*`、`\d+` 这类正则写法时给出提醒：默认的「字面量优先」模式会把它们当普通文字匹配，需要写成 `re:.*` 才是正则。

**变更**

- 确认弹窗区分语气：删除仍是红色的「修枝」告警，而套用默认值这类可逆改动改用绿色的「嫁接」样式，按钮也从「永久删除 / 保留条目」变为「套用默认值 / 保持不变」。

**修复**

- 可视化测试台文件尾部残留了一段被写坏的脚本片段。
- 可视化测试台的模板夹具与后端默认值不一致（日程条目的 Cron、群聊 / 用户限定条目的优先级与触发方式）。

## v1.3.0

主题不再跟着 AstrBot 走，背景的世界树也从装饰变成了真正的场景。

**新增**

- 界面主题可以独立选择，共四档：跟随 AstrBot、白昼、暗林、荧夜。选择保存在浏览器本地，下次打开管理台仍然生效，且首屏不会先闪一下白底。
- 新增「荧夜」主题：树干退成剪影，叶簇由内向外发出青绿荧光，林间还有九点缓慢明灭的萤火。开启系统的「减少动态效果」后，萤火与摆动都会静止。

**变更**

- 背景世界树改为占满整个视口：树冠越过上边缘、根系越过左右两侧，页面真正像是坐在树中；窄屏会自动收小比例，避免只看到两根粗枝。

**修复**

- 可视化测试台会被浏览器缓存住旧的样式与脚本，导致刚改完的界面看起来没有变化。

## v1.2.0

侧栏也长成了树：从上到下是一根树干，三块面板作为枝节挂在树干上，底部收束成根须。

**变更**

- 侧栏重做为「树干 + 枝芽」结构：左侧一条渐变树干贯穿整列，每块面板以一根小枝和一枚叶芽与树干相连，末端收出根须弧线。
- 树冠概览从 2×3 方格表改为「年轮总数 + 叶片标签」：总数卡带同心年轮纹理，其余统计变成叶形 chips，点击即按该条件筛选，文件夹数量为静态项并用虚线边框区分。
- 搜索与筛选区的四个下拉改为 2×2 栅格，重置项统一为「全部」，不再被列宽截断。
- 侧栏整体高度明显下降，长列表下可视条目更多。
- 窄屏（≤ 900px）自动隐藏树干与枝芽装饰，避免与折叠后的横向布局冲突。

**修复**

- 统计筛选项在鼠标停留时会被悬停样式盖掉选中态，看起来像没有生效。
- 深色模式下树冠概览与叶片标签中的数字对比度不足。

## v1.1.0

条目多了也能一眼找到目标，并把世界树的意象带进整个界面。

**新增**

- 条目排序与自动分组：优先级由小到大 / 由大到小、条目类型、文件夹、名称、最近更新、启用状态共 7 种排序，列表会按当前排序自动生成分组标题（如“树心 · 优先级 ≤ 10”“主干 · 优先级 11–50”）。
- 每个条目显示所属模板徽章，并可按模板筛选（常规 / 常驻 / 随机 / 日程 / 群聊限定 / 用户限定）。
- 顶部统计卡可直接点击筛选（全部 / 启用中 / 已停用 / 定时 / 限定范围）。
- 复制为新条目：以现有条目为模板快速派生，命名自动追加“副本”。
- 导出所选条目为单独的 JSON 文件。
- 拖拽 JSON 文件到页面即可导入。
- 紧凑视图与“收起全部”，长列表一屏能看到更多条目。
- 键盘快捷键：`/` 聚焦搜索、`N` 新建条目、`Ctrl+Enter` 保存、`Esc` 关闭弹层。
- 编辑器按“基础信息 / 触发方式 / 生效范围与时长 / 注入内容”分组，字段职责一目了然。
- 界面背景改为程序化生成的世界树：分形树冠、垂落的根系与林间雾气，并随 `prefers-reduced-motion` 自动停止摆动。

**修复**

- 深色模式下开关控件与图例文字对比度不足。
- 编辑抽屉在小屏上内容溢出、无法滚动到底。
- 只有一页结果时分页区域仍占据空白高度。
- 分页按钮缺少 `aria-current` 与可读的 `aria-label`。
- 移动端工具栏按钮换行错位。
- 导入弹窗打开后焦点未移入对话框。

## v1.0.2

- 将面向中文用户的插件展示名统一为“世界树”；内部包名、数据目录、命令组、Web API 和模型工具标识保持不变，升级不会产生新的数据副本。
- README 明确区分世界树管理台、管理员聊天命令、导入文件和可选模型工具的能力范围。

## v1.0.1

- 修复 AstrBot 插件页安全沙箱禁止原生 `window.confirm()`，导致单条和批量删除按钮看似无响应的问题。
- 删除确认改为世界树主题的页内对话框，支持取消、Esc、点击遮罩关闭、键盘焦点保护和窄屏布局。

## v1.0.0

首个独立发布版本。

- 使用独立插件名、类名、命令组、数据目录与 Web API 路由，可与上游插件并存。
- 新增原生 AstrBot Plugin Page：世界树风格的搜索、筛选、分页、批量操作、编辑、导入和导出。
- 将条目管理改为服务端检索与乐观修订号校验，避免大量条目逐个展开和多页面覆盖保存。
- 修复重复关键词命中重置已激活条目时长/次数的问题。
- 将当前会话的固定/屏蔽状态从全局 `scope` 中剥离，不再意外改写全局适用范围。
- 默认将内容作为本轮临时上下文注入，不污染会话历史；保留可选 `system_prompt` 模式。
- 新条目默认使用字面量关键词；正则需要显式写作 `re:<表达式>`，导入的上游格式自动保留旧正则语义。
- 新增“选择本页”、移动端折叠筛选、编辑器单条删除、深色主题和并发请求防串页。
- 新增默认关闭且仅管理员可见的 `worldtree_lore_add_entry` 模型工具，模型创建项会集中归档以便复核。
- 修复空文本消息无法继续使用已激活/Cron 条目、被屏蔽会话误消耗 Cron 信号、删除条目后遗留会话屏蔽 ID 等边缘问题。
- 限制服务器端临时导出文件数量，避免长期运行后无上限堆积。
