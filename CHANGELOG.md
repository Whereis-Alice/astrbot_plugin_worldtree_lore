# 更新日志

## v1.0.1

- 修复 AstrBot 插件页安全沙箱禁止原生 `window.confirm()`，导致单条和批量删除按钮看似无响应的问题。
- 删除确认改为世界树主题的页内对话框，支持取消、Esc、点击遮罩关闭、键盘焦点保护和窄屏布局。

## v1.0.0

首个独立发布版本。

- 使用独立插件名、类名、命令组、数据目录与 Web API 路由，可与上游插件并存。
- 新增原生 AstrBot Plugin Page：世界树风格的搜索、筛选、分页、批量操作、编辑、导入和导出。
- 将条目管理改为服务端检索与乐观修订号校验，避免大量条目逐个展开和多页面覆盖保存。
- 修复重复关键词命中重置已激活条目时长/次数的问题。
- 将当前会话的固定/屏蔽状态从全局 `scope` 中剥离，不再意外改写全局适用范围。
- 默认将内容作为本轮临时上下文注入，不污染会话历史；保留可选 `system_prompt` 模式。
- 新条目默认使用字面量关键词；正则需要显式写作 `re:<表达式>`，导入的上游格式自动保留旧正则语义。
- 新增“选择本页”、移动端折叠筛选、编辑器单条删除、深色主题和并发请求防串页。
- 新增默认关闭且仅管理员可见的 `worldtree_lore_add_entry` 模型工具，模型创建项会集中归档以便复核。
- 修复空文本消息无法继续使用已激活/Cron 条目、被屏蔽会话误消耗 Cron 信号、删除条目后遗留会话屏蔽 ID 等边缘问题。
- 限制服务器端临时导出文件数量，避免长期运行后无上限堆积。
